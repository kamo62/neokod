## Unreleased - 2026-07-04 (Patch)

Release impact: Patch because this fixes Codex sub-agent progress projection without changing public user workflows.

- Fixed Codex child-thread item completions so the Subagents panel receives durable worker progress rows instead of showing empty running cards.
- Added regression coverage for Codex child-thread progress mapping.
